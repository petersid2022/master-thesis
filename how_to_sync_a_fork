# 1. Fetch latest from upstream
git fetch upstream

# 2. Checkout your feature branch (local, not origin/master)
git checkout self-speculation-save-cache

# 3. Rebase your branch on top of upstream/master
git rebase upstream/master

# 4. Force push to update your PR (if already pushed)
git push origin self-speculation-save-cache --force
